# specify path to suppo.conf or leave blank
# default is the same as SuppoCore
#suppo_conf=/home/devel/.suppocore/suppo.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
